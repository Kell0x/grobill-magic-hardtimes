# Changes are read before the next death. Volumes and grain: 0.0 to 1.0.
enabled=true
durationSeconds=6.0
autoRespawn=true
reducedMotion=false
grain=0.7
heartbeat=0.65
breathing=0.45
staticVolume=0.35
# append = built-in + custom; replace = custom with a built-in fallback for empty sections.
phrases=append
